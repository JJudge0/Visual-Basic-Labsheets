Module Module1
    Sub Main()
        'Name: Jatinder Judge StudentID:1945979
        Console.WriteLine("Hello world") 'writes it on a new line
        Console.Write("Goodbye world") 'writes it on the same line
        ' This is just the testing phase of how to write in the console command line
    End Sub

End Module
